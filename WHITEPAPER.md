# TSagent 白皮书：让专业角色完成业务，而不是完成流程

## 摘要

TSagent 是面向长对话、复杂决策与高责任边界的自治专业智能体架构。它不以“所有节点是否执行”“工具是否被调用”“字段是否填齐”评判成功，而以用户是否得到岗位独有、可继续推进、来源边界明确的业务成果评判成功。

Eleanor 的演进说明：角色能力并不等于一份提示词；它来自语义理解、目标循环、工具价值合同、运行连续性、非破坏性交付和真实会谈验收的组合。

## 一、第一原则：阶段成果优先

用户通常不会一次说清金额、期限、目标、保单和偏好。正确系统不应把“不完整”翻译成“不能产生价值”。角色必须先回答：

1. 当前已确认的事实改变了什么？
2. 本岗位现在能留下什么条件性判断或行动边界？
3. 还缺的究竟是用户独有事实、可检索外部事实、静态计算，还是另一岗位判断？
4. 哪一个缺口真的会改变下一步？

只有回答完前三项后，才允许追问、委托或停止。

## 二、角色是自治单元，不是流程节点

每个角色读取的业务输入是自然语言工作记忆：最新用户表达、更正、已批准角色报告、Research Brief、工具证据与未决问题。角色输出是：

- `professional_detail`：完整岗位成果；
- `collaboration_brief`：可选协作、待核验事项或等待理由；
- 技术审计：预算、版本、幂等、调用结果和失败类别。

跨角色不传递私有业务 JSON。技术状态可以结构化，专业含义必须可读、可审计、可被下一角色重新理解。

## 三、会议不是固定顺序，而是角色租约

编排器不负责专业判断。它管理：当前事件是否使旧前提失效、哪位角色有新增价值、是否续用当前角色租约、何时等待用户、如何从角色技术失败恢复。

用户回复当前角色提出的关键问题时，优先续用该角色租约；不要每句话都重新举行“谁最适合”的编排投票。编排器若不执行角色协作请求，必须留下可见的执行理由，并把业务决定权交回原角色。

会议在等待用户时仍然开放。技术失败只能中断本轮未完成工作，不能自动关闭用户的长期咨询。

## 四、工具是证据能力，不是流程步骤

工具调用必须满足：

```text
角色先形成岗位主成果
→ 工具的不同结果会改变当前行动
→ 角色说明低结果、高结果各自如何改变行动
→ 权限、Manifest、参数编译和网关确认同一授权事实
→ 工具返回 Evidence Envelope
→ 角色解释证据、限制与对方案的影响
```

工具不负责推荐产品、风险放行或替代角色判断。工具失败后，角色保留现有条件性方案，并说明什么未能核验。

## 五、质量与人格化是交付层，不是第二个业务裁决者

安全审计只治理危险行为：编造事实、伪造来源、关键事实冲突、越权承诺、安全或权限破坏。它可以产生修复反馈，但不能因为格式、交付投影或流程未走全而删除角色已经完成的业务成果。

人格化层的职责是无损润色：保留专业正文的事实、数字、条件、限制、结论、行动顺序与问题；只调整称呼、段落、连接句和角色语气。两次润色仍明显丢失内容时，交付原专业正文，而不是生成泛化 fallback。

## 六、运行可靠性与真实验收

会谈快照由版本协调器在完整回合后提交。前端只消费已提交版本，避免用户、数据库、Redis 和浏览器各自看到不同进度。角色失败写入技术连续性事件；恢复逻辑只使用已有用户表达、批准报告和证据，不把失败文本当作业务事实。

验收必须使用真实模型、真实前端协议或真实角色运行时，并保留：用户原文、角色原文、专业正文、工具意图/授权/Manifest/网关审计、版本与恢复事件。评分站在用户和职业审查者立场，不按字段命中率判定好坏。

## 结论

专业智能体的核心不是更多 LLM 调用，而是让角色在不确定性中持续留下可验证的职业价值：角色独立、协作自然、工具提供证据、系统保障连续性、用户始终拿得到已完成的成果。
