> [← 返回首页](README.md) · [阅读目录](README.md#阅读目录) · [下一篇：系统架构 →](docs/ARCHITECTURE.md)

# TSagent 白皮书
## 基于自治专业角色协作的买方投顾智能体系统

**版本：** v1.0  
**定位：** 技术与架构白皮书  
**范围：** 角色自治、会谈协作、工作记忆、工具证据闭环、运行治理与评估；不构成投资建议或产品说明书。

---

## 摘要

财富管理咨询不是一次“推荐产品”的问答，而是目标、期限、现金流、风险承受、家庭责任、公开事实和不确定性之间的持续权衡。单一模型容易把这些问题压缩为一次回答；固定流程多智能体则容易把专家协作简化成角色轮流发言。

TSagent 将买方投顾理解为一个**可审查的专业协作过程**：金融顾问、理财规划师、风险分析师、首席财务官和数据分析师各自有不可替代的职责；角色只依赖自然语言工作记忆完成本轮判断；编排层按新增专业价值选择下一位角色；工具只提供证据，不替角色裁决；系统在来源、预算、权限和失败条件上保持可治理。

它的核心创新不是更多角色，而是让每个角色满足独立性：被从会议系统中取出后，只提供自然语言上下文和授权能力，仍能理解问题、守住职责边界、完成专业成果，并写出可被其他角色继续理解的报告。

---

## 1. 问题定义

### 1.1 单角色理财问答的局限

单一模型通常会同时承担目标澄清、事实核验、风险审查、方案设计和最终表述。这会造成：

- 目标与风险边界容易混在同一段建议中；
- 未核验的外部事实可能被当作结论；
- 用户更正后，旧前提仍悄悄影响回答；
- 没有独立审查机制，乐观方案缺少反方制衡；
- 工具调用和最终建议之间缺乏可解释的关系。

### 1.2 固定流程多智能体的局限

把角色排成“先 A、后 B、最后 C”的会议图，并不等于拥有专业协作。常见退化包括：

- 任一角色都依赖固定上游输出；
- 信息不完整时默认不断追问用户；
- 角色为了存在感重复已有结论；
- 工具按关键词或字段齐全度机械触发；
- 业务判断被私有状态字段或规则节点锁死。

### 1.3 设计目标

TSagent 的目标不是自动替用户做投资决策，而是交付一个更接近买方投顾工作方式的过程：

1. 明确当前真正要决定什么；
2. 区分用户事实、角色判断、外部证据和假设；
3. 让风险角色独立挑战乐观方案；
4. 让规划与资本角色在约束内给出条件性路径；
5. 在没有新增价值时停止，而不是继续消耗时间和 Token。

---

## 2. 设计原则

### 2.1 角色边界保护

每个角色有自己的岗位使命、主成果、输入边界、输出边界和停止条件。角色不因其他角色的结论而放弃自己的专业判断，也不越权替他人完成职责。

### 2.2 语义层负责业务，规则层负责治理

系统明确区分两类问题：

| 语义层应决定 | 治理层可规则化 |
|---|---|
| 本轮真正的专业问题 | 权限与同意 |
| 角色是否有新增贡献 | Schema 与输入合法性 |
| 是否需要追问、协作、数据核验或工具 | 预算、超时、重试、幂等 |
| 报告中的专业判断与条件方案 | 来源约束、审计与失败关闭 |

这意味着系统不使用关键词命中、字段完整度或固定角色顺序来决定业务结论；但仍使用明确的运行规则保护安全和稳定性。

### 2.3 模块化推进，而非固定会议图

角色协作以事件为中心。任何角色完成报告、数据分析师返回证据、工具失败或用户更正，都可能成为下一次协调的触发事件。编排层不要求所有角色参与，也不假设最终一定由某个角色收尾。

### 2.4 金融理论与证据边界

系统关注的是资金用途、期限错配、流动性、家庭资产负债表、回撤承受、集中度和执行顺序等可解释的买方视角；对于产品条款、市场事实和外部规则，必须明确来源、时点、适用范围和未核验边界。

---

## 3. 系统架构

架构由五部分组成：自治专业角色、协作事件层、工作记忆构造器、低频编排层和工具/运行治理层。详细说明见 [ARCHITECTURE.md](docs/ARCHITECTURE.md)。

```text
用户与外部证据
  → 来源化协作事件
  → 工作记忆装配与上下文治理
  → 编排器选择 act / wait / stop
  → 一个角色独立完成主成果
  → 必要时进入一次受控工具闭环
  → 自然语言报告与协作 Brief 回流
```

---

## 4. 关键创新

### 4.1 主成果合同：从“角色功能”转向“岗位交付”

创新：先定义角色本轮必须留下的专业价值，再设计提示词、工具和流程。

原因：真实专业工作不是收集字段，而是在信息不完整时依然指出边界、条件、冲突和下一步。

差异：传统 Agent 常以“是否完成任务列表”定义成功；这里以“用户是否获得岗位独有、可推进的成果”定义成功。

### 4.2 双层输出：主成果 + 后续协作

创新：角色报告同时包含已完成的专业主成果和可选的协作 Brief。

原因：委托他人不代表本轮无价值。把两者混在一起会使系统误判角色只会转交任务。

差异：每次角色发言都可以既推进当前判断，又给出下一位最合适专家的自然语言请求。

### 4.3 自然语言协作合同

创新：跨角色不依赖 `profile`、`plan`、`risk_output` 一类私有业务字段；角色通过报告、协作请求和带来源证据进行交接。

原因：专业角色应可被单独抽取、替换或扩展；私有字段会把业务理解锁死在会议图和实现细节中。

差异：技术 JSON 只承载预算、超时、幂等、审计 ID 和 feature flag，不承载跨角色业务结论。

### 4.4 低频事件驱动编排

创新：编排器只在新事件、事实更正、证据回流或角色完成后做有限语义协调。

原因：高频“角色发言后再问一次编排器”会造成重复 LLM 调用、延迟和空转。

差异：编排器只决定 `act / wait / hold` 与角色归属；角色仍独立决定专业内容。`hold` 表示当前没有新增动作，但会谈可在用户补充、跨时间继续或技术恢复后推进，不是系统关闭咨询。

### 4.5 用户更正优先与上下文污染控制

创新：把用户最新更正视为会使旧前提失效的事件，而不是历史最后一条消息。

原因：理财场景中金额、期限、用途和可动用性改变，会使历史计划和风险判断整体失效。

差异：压缩优先丢弃已解决事项和过程细节，不丢弃最新更正、关键事实、证据边界和未解决冲突。

### 4.6 反事实工具授权与 Evidence Envelope

创新：工具调用前要求角色说明“低结果与高结果是否改变本岗位行动”；工具调用后只返回事实、假设、限制和缺失证据。

原因：公式和数据接口本身不等于决策价值；工具输入也不能伪装为工具输出。

差异：工具是中立能力，不是流程节点；角色才是解释证据、形成报告和承担专业责任的主体。

### 4.7 技术连续性与有限质量门

创新：工具失败、来源不足或运行超时时，保留已验证事实支持的最小条件性成果；质量门只硬拦截幻觉、伪造证据、明确冲突、越权与安全破坏。一次技术失败只形成连续性事件，不关闭整场长期会谈。

原因：用户最差的体验不是“系统说不确定”，而是等待很久后被内部审核吞掉一个原本合理的专业结论。

差异：不把“流程未跑全”当成失败，也不让自由重写掩盖工具或来源失败；最终交付只能无损润色已批准的专业正文，不能把它压缩成泛化安全话术。

### 4.8 会谈租约、完整快照与统一授权

创新：角色在职责未完成时持有可恢复的会谈租约；用户回答其关键问题时默认回到该角色。系统以单调递增版本提交完整会谈快照，工具展示、Manifest、桥接与网关读取同一授权事实。

原因：每句话都重新路由会抢占角色的专业连续性；多个存储进度混用会让前后端各自看到不同会谈；权限多头读取会导致“角色看得到工具但实际不能用”。

差异：版本协调器只处理提交、幂等、过期写入和恢复，不参与业务判断；开发全局授权也不被误写成生产用户授权。

---

## 5. 跨模块依赖与演进

为了避免多智能体平台在扩角色、扩工具时重新耦合，系统采用以下依赖原则：

- **角色依赖能力描述，不依赖工具实现细节。** 角色看到的是授权能力名片与 Skill。
- **工具依赖显式输入，不读取角色私有 state。** 工具服务可在会议图之外独立发现、调用和审计。
- **事件是稳定交接面。** 事件版本、幂等键和审计 ID 支持回放与恢复，但业务内容保持自然语言可读。
- **使用显式合同替代隐式占位。** 新模块尚未接入时，应声明其输入、输出、失败语义与版本边界，而非用空字段假装完成。
- **特性开关只用于灰度，不定义业务。** 新自治路径可以并行验证，但开关不能决定风险判断或方案结论。

---

## 6. 评估与展望

### 6.1 评估原则

评估应站在用户和首席专业审查者的视角，而不是只看断言通过率：

- 是否真正解决了用户当前的决策问题？
- 角色是否产出了职责内的独有价值？
- 是否尊重最新更正与来源边界？
- 是否应调用工具却没调用，或不应调用却机械调用？
- 失败后是否仍保留可交付价值并明确限制？
- 角色协作是否减少绕路，而非增加发言和耗时？

完整框架见 [EVALUATION.md](docs/EVALUATION.md)。

### 6.2 后续方向

未来可以继续建设：生产级身份与租户隔离、独立 MCP 服务包、外部证据评级、长期记忆的可撤销与可审计机制、跨实例事件恢复、模型供应商路由与成本治理，以及面向不同专业领域的角色模板。

---

## 结语

多智能体系统的价值不在于“让更多模型讲话”，而在于把专业分工、证据责任、上下文连续性和运行治理组织成一个可扩展的协作系统。

TSagent 的核心立场是：**角色独立，协作自然；工具提供证据，角色承担判断；编排协调过程，不取代专业。**

---

> [← 返回首页](README.md) · [阅读目录](README.md#阅读目录) · [下一篇：系统架构 →](docs/ARCHITECTURE.md)
